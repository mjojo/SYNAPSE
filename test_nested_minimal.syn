// Minimal test: nested if inside while with <
fn main() {
    let pos = 0
    
    while pos < 2 {
        let c = 52
        
        print(1001)
        print(c)
        
        // Nested if with >
        if c > 47 {
            print(2001)
            // Nested if with < inside the first if
            if c < 58 {
                print(2002)
            }
        }
        
        pos = pos + 1
    }
    
    return 0
}
