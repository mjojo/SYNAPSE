// DEBUG V3 - Step 3
let src_mem = 0
let data_mem = 0
let code_buf = 0
let src_len = 0
let code_idx = 0

fn init_memory() {
    src_mem = alloc(10000)
    print(888888)
    print(src_mem) // Check address
    data_mem = alloc(50000)
    code_buf = alloc_exec(64000)
}

fn main() {
    print(111)
    init_memory()
    print(222)
    
    // Test set_byte
    let b = src_mem
    set_byte(b, 0, 102) 
    print(333)
    
    let c = get_byte(b, 0)
    print(444)
    print(c)
    
    return 0
}
