// Test with global like in self_compile
let data_mem = 0

fn main() {
    let dm = 0
    dm = alloc(16000)
    data_mem = dm
    
    // Write at index 0
    let i = 0
    dm[i] = 42
    
    // Read back
    let v = dm[i]
    
    puts("Wrote 42 at [0], read back:")
    print(v)
    
    return 0
}
