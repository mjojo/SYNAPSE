fn main() {
    window(800, 600)
    let running = 1
    let counter = 0
    while running {
        counter = counter + 1
        if counter == 10000000 {
            running = 0
        }
    }
    print(counter)
    return 0
}
