fn main() {
    print(111)
    let a = 200
    let b = 100
    
    // a > b (200 > 100) Expect 1
    if a > b { print(1) } else { print(0) }
    
    // a < b (200 < 100) Expect 0 (or 1 if < is >)
    if a < b { print(2) } else { print(3) }
    
    return 0
}
