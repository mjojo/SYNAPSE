# TITAN Language — Справочник команд

> Краткий справочник всех команд TITAN v0.18.0
> © 2025 mjojo & GLK-Dev

---

## 📋 Команды REPL

| Команда | Описание | Пример |
|---------|----------|--------|
| `RUN` | Запустить программу | `RUN` или `RUN 100` |
| `LIST` | Показать листинг | `LIST` или `LIST 10-50` |
| `NEW` | Очистить программу | `NEW` |
| `SAVE` | Сохранить в файл | `SAVE "game.bas"` |
| `LOAD` | Загрузить из файла | `LOAD "game.bas"` |
| `EXIT` | Выйти из TITAN | `EXIT` |
| `HELP` | Показать справку | `HELP` или `HELP PRINT` |

---

## 📝 Основные команды

### Ввод/Вывод

| Команда | Описание | Пример |
|---------|----------|--------|
| `PRINT` | Вывод на экран | `PRINT "Hello"` |
| `INPUT` | Ввод с клавиатуры | `INPUT "Name: ", N$` |
| `CLS` | Очистить экран | `CLS` |

### Переменные

| Команда | Описание | Пример |
|---------|----------|--------|
| `LET` | Присвоить значение | `LET A = 10` или `A = 10` |
| `DIM` | Объявить массив на куче | `DIM A(100000)` |

### Управление

| Команда | Описание | Пример |
|---------|----------|--------|
| `GOTO` | Безусловный переход | `GOTO 100` |
| `IF...THEN` | Условие | `IF A > 10 THEN PRINT "Yes"` |
| `GOSUB` | Вызов подпрограммы | `GOSUB 1000` |
| `RETURN` | Возврат из подпрограммы | `RETURN` |
| `END` | Конец программы | `END` |
| `STOP` | Остановить выполнение | `STOP` |

### Функции (Phase 12)

| Команда | Описание | Пример |
|---------|----------|--------|
| `FUNC` | Начало именованной функции | `100 FUNC FACTORIAL` |
| `ENDFUNC` | Конец функции | `200 ENDFUNC` |
| `LOCAL` | Объявление локальных переменных | `LOCAL A, B, N` |

### FFI — Foreign Function Interface (Phase 13)

| Команда | Описание | Пример |
|---------|----------|--------|
| `MSGBOX` | Показать Windows MessageBox | `MSGBOX "Hello", "Title"` |

### GDI Graphics (Phase 14)

| Команда | Описание | Пример |
|---------|----------|--------|
| `GDI WINDOW w, h` | Создать окно | `GDI WINDOW 800, 600` |
| `GDI PIXEL x, y, c` | Нарисовать пиксель | `GDI PIXEL 100, 100, 255` |
| `GDI LINE x1,y1,x2,y2,c` | Нарисовать линию | `GDI LINE 0, 0, 100, 100, 65535` |
| `GDI SHOW` | Показать окно | `GDI SHOW` |

### Бинарные файлы (Phase 17)

| Команда | Описание | Пример |
|---------|----------|--------|
| `BLOAD "file", A` | Загрузить бинарный файл в массив | `BLOAD "weights.bin", W` |
| `BSAVE "file", A` | Сохранить массив в бинарный файл | `BSAVE "data.bin", A` |

### Матричные операции (Phase 18)

| Команда | Описание | Пример |
|---------|----------|--------|
| `MATMUL C, A, B, m, k, n` | Умножение матриц (AVX2+FMA) | `MATMUL H, I, W, 1, 784, 128` |
| `VRELU B, A` | ReLU активация (SIMD) | `VRELU H, H` |

### Циклы

| Команда | Описание | Пример |
|---------|----------|--------|
| `FOR...TO` | Цикл со счётчиком | `FOR I = 1 TO 10` |
| `NEXT` | Конец цикла FOR | `NEXT I` |

### Комментарии

| Команда | Описание | Пример |
|---------|----------|--------|
| `REM` | Комментарий | `REM Это комментарий` |

---

## 🔧 Отладка (TITAN)

| Команда | Описание | Пример |
|---------|----------|--------|
| `DUMP` | Показать JIT байт-код | `DUMP` |
| `VARS` | Показать переменные | `VARS` |
| `REGS` | Показать регистры CPU | `REGS` |

---

## 📐 Встроенные функции

### Математика

| Функция | Описание | Пример |
|---------|----------|--------|
| `ABS(x)` | Абсолютное значение | `ABS(-5)` → `5` |
| `SGN(x)` | Знак (-1, 0, 1) | `SGN(-5)` → `-1` |
| `INT(x)` | Целая часть | `INT(3.7)` → `3` |
| `SQR(x)` | Квадратный корень | `SQR(16)` → `4` |
| `SIN(x)` | Синус (радианы) | `SIN(0)` → `0` |
| `COS(x)` | Косинус | `COS(0)` → `1` |
| `TAN(x)` | Тангенс | `TAN(0)` → `0` |
| `ATN(x)` | Арктангенс | `ATN(1)` → `0.785...` |
| `LOG(x)` | Натуральный логарифм | `LOG(2.718)` → `1` |
| `EXP(x)` | Экспонента (e^x) | `EXP(1)` → `2.718...` |
| `RND(x)` | Случайное число 0..1 | `RND(1)` → `0.123...` |

### Строки

| Функция | Описание | Пример |
|---------|----------|--------|
| `LEN(s$)` | Длина строки | `LEN("Hello")` → `5` |
| `LEFT$(s$,n)` | Левые n символов | `LEFT$("Hello",2)` → `"He"` |
| `RIGHT$(s$,n)` | Правые n символов | `RIGHT$("Hello",2)` → `"lo"` |
| `MID$(s$,p,n)` | Подстрока | `MID$("Hello",2,3)` → `"ell"` |
| `CHR$(n)` | Символ по коду | `CHR$(65)` → `"A"` |
| `ASC(s$)` | Код символа | `ASC("A")` → `65` |
| `STR$(n)` | Число → строка | `STR$(42)` → `"42"` |
| `VAL(s$)` | Строка → число | `VAL("42")` → `42` |
| `INSTR(s$,t$)` | Поиск подстроки | `INSTR("Hello","ll")` → `3` |

---

## 🎯 Операторы

### Арифметические
| Оператор | Описание | Пример |
|----------|----------|--------|
| `+` | Сложение | `5 + 3` → `8` |
| `-` | Вычитание | `5 - 3` → `2` |
| `*` | Умножение | `5 * 3` → `15` |
| `/` | Деление | `15 / 3` → `5` |
| `^` | Степень | `2 ^ 3` → `8` |

### Сравнения
| Оператор | Описание | Пример |
|----------|----------|--------|
| `=` | Равно | `A = 5` |
| `<>` | Не равно | `A <> 5` |
| `<` | Меньше | `A < 5` |
| `>` | Больше | `A > 5` |
| `<=` | Меньше или равно | `A <= 5` |
| `>=` | Больше или равно | `A >= 5` |

### Логические
| Оператор | Описание | Пример |
|----------|----------|--------|
| `AND` | Логическое И | `A > 0 AND B > 0` |
| `OR` | Логическое ИЛИ | `A > 0 OR B > 0` |
| `NOT` | Логическое НЕ | `NOT A > 0` |

---

## 💡 Примеры

### Вывод текста
```basic
10 PRINT "Hello, World!"
```

### Ввод числа
```basic
10 INPUT "Enter your age: ", AGE
20 PRINT "You are "; AGE; " years old"
```

### Цикл
```basic
10 FOR I = 1 TO 5
20   PRINT I; " squared = "; I * I
30 NEXT I
```

### Условие
```basic
10 INPUT "Number: ", N
20 IF N > 0 THEN PRINT "Positive"
30 IF N < 0 THEN PRINT "Negative"
40 IF N = 0 THEN PRINT "Zero"
```

---

*Версия: 0.18.0 | TITAN Language*

---

## 📚 Примеры

### Neural Network Inference (MNIST)
```basic
REM TITAN Neural Engine - распознавание цифр
DIM A(100352)    ; W1: 784x128 weights
DIM B(128)       ; B1: biases
DIM C(1280)      ; W2: 128x10 weights  
DIM D(10)        ; B2: biases
DIM I(784)       ; Input image
DIM H(128)       ; Hidden layer
DIM O(10)        ; Output (10 digits)

BLOAD "w1.bin", A
BLOAD "b1.bin", B
BLOAD "w2.bin", C
BLOAD "b2.bin", D
BLOAD "img.bin", I

REM Layer 1: Input -> Hidden
MATMUL H, I, A, 1, 784, 128
FOR J = 0 TO 127
LET H(J) = H(J) + B(J)
NEXT J
VRELU H, H

REM Layer 2: Hidden -> Output
MATMUL O, H, C, 1, 128, 10
FOR K = 0 TO 9
LET O(K) = O(K) + D(K)
NEXT K

REM Result: O(9) = 9.299798 → Digit 9!
```

### Mandelbrot Fractal (GDI)
```basic
GDI WINDOW 400, 300
FOR Y = 0 TO 299
FOR X = 0 TO 399
  LET C = 0
  REM ... fractal computation ...
  GDI PIXEL X, Y, C
NEXT X
NEXT Y
GDI SHOW
```

### MessageBox
```basic
MSGBOX "Hello World!", "TITAN"
MSGBOX A$, "Title"
MSGBOX "Hello, " + N$ + "!", "Greeting"
```

### Рекурсия (Факториал)
```basic
10 LET N = 5
20 GOSUB 100
30 PRINT "5! = "; R
40 END
100 FUNC FACT
110 LOCAL R
120 IF N <= 1 THEN LET R = 1 : RETURN
130 LET R = N : LET N = N - 1
140 GOSUB 100
150 LET N = N + 1 : LET R = R * N
160 RETURN
170 ENDFUNC
```
