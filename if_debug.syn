// if_debug.syn - Debug if condition

fn check(n) {
    print(n)            // Show parameter
    let cond = n < 2
    print(cond)         // Show condition result
    
    if (n < 2) {
        return 100
    }
    return 200
}

fn main() {
    print(999)
    print(check(1))
    return 0
}
