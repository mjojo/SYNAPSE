// lt_test.syn - Test less than with parameter

fn check(n) {
    let result = n < 2
    return result
}

fn main() {
    print(check(1))     // Should be 1 (true)
    print(check(5))     // Should be 0 (false)
    return 0
}
