# ============================================================================
# SYNAPSE v1.1 - Control Flow Roadmap
# Путь к Тьюринг-полноте и Self-Hosting
# ============================================================================

## 🎯 Phase 6.1: Логика - Фундамент Принятия Решений

### Текущий Статус (v1.0)
- ✅ Токены: `if`, `elif`, `else`, `while`, `loop` уже определены
- ✅ AST узлы: NODE_IF, NODE_WHILE, NODE_BLOCK существуют  
- ✅ Операторы сравнения: ==, !=, <, >, <=, >= готовы
- ✅ Лексер: распознаёт ключевые слова через `synlex_check_keyword`
- ⚠️ Парсер: частично готов (let, fn), нужна логика для блоков
- ❌ Кодогенерация: нет JIT для условных переходов (CMP, JE, JMP)

### Что Необходимо Реализовать

#### 1. Парсер (Parser v2 Extension)
**Файл:** `src/parser_v2.asm`

Добавить функции:
- `parse_if_statement()` - разбор if/elif/else
- `parse_while_statement()` - разбор while  
- `parse_condition()` - разбор условий (a > b, x == 0)
- `parse_block()` - рекурсивный разбор блоков { ... }

**Структура AST для условий:**
```
NODE_IF
  ├─ condition: NODE_BINOP (SOP_GT)
  │   ├─ left: NODE_IDENT ("x")
  │   └─ right: NODE_NUMBER (0)
  ├─ then_block: NODE_BLOCK
  │   └─ statements...
  └─ else_block: NODE_BLOCK (опционально)
      └─ statements...
```

#### 2. Кодогенератор (JIT Codegen Update)
**Файл:** `src/jit_core.asm` (или новый `src/jit_control_flow.asm`)

Реализовать:
- **Метки (Labels):** система именованных адресов в коде
- **Сравнения:** генерация CMP/TEST инструкций
- **Условные переходы:** JE, JNE, JG, JL, JGE, JLE
- **Безусловные переходы:** JMP для циклов

**Пример генерации для `if x > 0:`**
```asm
; Загрузить x в регистр
mov rax, [x_addr]
; Сравнить с 0
cmp rax, 0
; Если меньше или равно, прыгнуть в else
jle .else_label
; Код then-блока
...
jmp .end_if
.else_label:
; Код else-блока (если есть)
...
.end_if:
```

**Пример генерации для `while i < 10:`**
```asm
.loop_start:
mov rax, [i_addr]
cmp rax, 10
jge .loop_end    ; Если i >= 10, выход
; Тело цикла
...
jmp .loop_start  ; Прыжок назад
.loop_end:
```

#### 3. Система Управления Метками
**Новая структура данных:**
```asm
struct Label {
    name: string        ; "loop_start", "if_else_1"
    address: qword      ; Абсолютный адрес в JIT-буфере
    resolved: bool      ; Метка уже определена?
    fixup_list: ptr     ; Список неразрешённых переходов
}
```

Функции:
- `label_create(name)` - создать метку
- `label_define(name, addr)` - установить адрес метки
- `label_reference(name)` - сослаться на метку (для JMP)
- `label_fixup()` - исправить все неразрешённые адреса

#### 4. Таблица Символов (Variable Tracking)
**Проблема:** Условия работают с переменными (`if x > 0`)

Нужна таблица символов для хранения:
```asm
struct Variable {
    name: string        ; "x", "counter", "result"
    type: u8           ; int, f64, ptr
    address: qword     ; Адрес в стеке или Merkle-памяти
    scope: u16         ; Уровень вложенности блока
}
```

Функции:
- `var_declare(name, type)` - объявить переменную
- `var_lookup(name)` - найти переменную по имени
- `var_get_address(name)` - получить адрес для доступа
- `scope_enter()` / `scope_exit()` - управление областями видимости

---

## 🗺️ Дорожная Карта: v1.0 → v2.0

### v1.1 - Условия (Q1 2025)
- ✅ if / elif / else парсинг
- ✅ Операторы сравнения в выражениях
- ✅ JIT-генерация условных переходов
- ✅ Простые тесты (factorial, max, abs)

### v1.2 - Циклы (Q2 2025)
- ✅ while парсинг и генерация
- ✅ loop (бесконечный цикл)
- ✅ break / continue  
- ✅ Тесты: сумма массива, поиск, обучение нейросети (epochs)

### v1.3 - Функции (Q2 2025)
- ✅ fn парсинг с параметрами
- ✅ Таблица символов для локальных переменных
- ✅ Стековые фреймы (PUSH/POP RBP)
- ✅ Возврат значений (return)
- ✅ Рекурсия (факториал, Фибоначчи)

### v1.4 - Типы (Q3 2025)
- ✅ Система типов: int, f32, f64, bool, string
- ✅ Проверка типов в парсере
- ✅ Автоматическое приведение типов (int → f64)
- ✅ Массивы фиксированного размера

### v1.5 - Структуры (Q3 2025)
- ✅ struct определения
- ✅ Доступ к полям через точку (obj.field)
- ✅ Выравнивание памяти
- ✅ Вложенные структуры

### v1.6 - Строки (Q4 2025)
- ✅ Строковые литералы
- ✅ Операции: concat, substr, length, compare
- ✅ UTF-8 поддержка
- ✅ Интерполяция переменных

### v1.7 - Файловый I/O (Q4 2025)
- ✅ file.read(), file.write()
- ✅ Работа с путями
- ✅ Буферизация
- ✅ Обработка ошибок

### v1.8 - Модули (Q1 2026)
- ✅ import system
- ✅ Пространства имён
- ✅ Компиляция нескольких файлов

### v1.9 - Препроцессор (Q1 2026)
- ✅ Макросы
- ✅ Условная компиляция (#if DEBUG)
- ✅ Include system

### v2.0 - Self-Hosting (Q2 2026)
- ✅ Переписать lexer на SYNAPSE
- ✅ Переписать parser на SYNAPSE
- ✅ Переписать codegen на SYNAPSE
- ✅ Компилировать synapse.exe самим собой
- 🎉 **НЕЗАВИСИМОСТЬ ОТ FASM**

---

## 🧪 Тестовые Сценарии для v1.1

### Тест 1: Простое условие
```synapse
fn abs(x: f64) -> f64:
    if x < 0:
        return -x
    else:
        return x
```

### Тест 2: Множественные условия
```synapse
fn grade(score: int) -> string:
    if score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    else:
        return "F"
```

### Тест 3: Факториал (рекурсия + условие)
```synapse
fn factorial(n: int) -> int:
    if n <= 1:
        return 1
    else:
        return n * factorial(n - 1)
```

### Тест 4: Защищённое условие (MOVA Engine)
```synapse
contract fn validate_transaction(amount: f64) -> bool:
    let balance: f64 = 1000.0
    
    if amount > balance:
        return false  # Недостаточно средств
    
    # Проверка целостности
    let hash_before = merkle_root()
    balance = balance - amount
    let hash_after = merkle_root()
    
    if verify_transition(hash_before, hash_after):
        return true   # Транзакция валидна
    else:
        return false  # Атака обнаружена
```

---

## 📊 Приоритет Реализации (Immediate Next Steps)

### Неделя 1-2: Парсер
1. Добавить `parse_condition()` - разбор `a > b`, `x == 0`
2. Добавить `parse_if_statement()` - разбор if/elif/else
3. Тесты: парсинг без выполнения

### Неделя 3-4: Кодогенерация
1. Система меток (Label Manager)
2. Генерация CMP/TEST
3. Генерация условных переходов (JE, JNE, JG, JL)
4. Тесты: простые условия (max, abs)

### Неделя 5-6: Циклы
1. Парсинг `while`
2. Генерация меток для начала/конца цикла
3. Обработка `break`/`continue`
4. Тесты: сумма, факториал (итеративный)

### Неделя 7-8: Интеграция
1. Обновление существующих тестов
2. Документация
3. Примеры для AI Flight Recorder
4. Benchmarks

---

## 🛡️ Философия: "Код как Песня"

**Принципы проектирования SYNAPSE:**

1. **Читаемость превыше всего**
   - Нет скобок `{}` - только отступы (как Python)
   - Явные типы: `let x: int = 10`
   - Понятные имена: `merkle_root()` вместо `mr()`

2. **Безопасность по умолчанию**
   - Все изменения памяти фиксируются в Merkle Tree
   - Невозможно "тихо" изменить переменную извне
   - Криптографическая верификация каждого состояния

3. **Производительность через прозрачность**
   - JIT компиляция в нативный x64
   - AVX2 оптимизация для тензорных операций
   - Нет VM overhead - прямое исполнение

4. **Декларативная логика**
   ```synapse
   # Вместо:
   if (x > 0 && x < 100 && verify(x))
   
   # Пишем:
   if x in range(0, 100) and verified(x):
   ```

5. **Композиция через пайплайны**
   ```synapse
   # Вместо:
   let tmp1 = preprocess(data)
   let tmp2 = normalize(tmp1)
   let result = neural_net(tmp2)
   
   # Пишем:
   let result = data |> preprocess |> normalize |> neural_net
   ```

---

## 🎵 Vision: "Симфония Логики" (v2.0+)

Когда SYNAPSE достигнет v2.0 (Self-Hosted), код будет выглядеть так:

```synapse
# Компилятор SYNAPSE, написанный на SYNAPSE

module compiler.parser:
    
    fn parse_if_statement() -> ASTNode:
        expect(TOKEN_IF)
        
        let condition = parse_expression() 
            |> validate_boolean
            |> optimize_condition
        
        expect(TOKEN_COLON)
        let then_block = parse_block()
        
        let else_block = if next_is(TOKEN_ELSE):
            parse_block()
        else:
            null
        
        return ASTNode.if_node(
            condition: condition,
            then: then_block,
            else: else_block
        ) |> annotate_types
          |> check_reachability
          |> seal_in_merkle  # Защита AST от подделки
```

**Это не просто код. Это поэзия, которую понимает машина.** 🎼

---

## 📝 Следующие Шаги

1. ✅ Создать примеры с управляющими конструкциями
2. ⚠️ Обновить парсер для условий
3. ⏳ Реализовать JIT-генерацию условных переходов
4. ⏳ Написать тесты для if/while
5. ⏳ Обновить документацию

**Готовы начать реализацию парсера условий?**
