fn main() {
    // Create "MZ" in buffer
    let buf = alloc(4)
    set_byte(buf, 0, 77)
    set_byte(buf, 1, 90)
    set_byte(buf, 2, 0)
    
    // Create filename "test.bin\0"
    let fname = alloc(2)
    fname[0] = 8392585648475956596
    fname[1] = 0
    
    // Write to file
    print(1)
    let h = fopen(fname, 1)
    print(h)
    let written = fwrite(h, buf, 2)
    print(written)
    fclose(h)
    print(2)
    
    return 0
}
