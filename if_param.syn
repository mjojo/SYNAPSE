// if_param.syn - Test if with parameter

fn check(n) {
    if (n < 2) {
        return 100
    }
    return 200
}

fn main() {
    print(check(1))     // Should be 100
    print(check(5))     // Should be 200
    return 0
}
