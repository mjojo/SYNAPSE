// Test < inside while (not nested)
fn main() {
    let s = "42"
    let pos = 0
    let len = 2
    
    while pos < len {
        let c = get_byte(s, pos)
        
        print(1001)
        print(c)
        
        // First test c > 47
        if c > 47 {
            print(2001)
        }
        
        // Then test c < 58 separately (not nested!)
        if c < 58 {
            print(2002)
        }
        
        pos = pos + 1
    }
    
    return 0
}
